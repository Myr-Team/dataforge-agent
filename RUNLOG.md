# Run Log

| Time | WP | Action | Result |
|---|---|---|---|
| 2026-06-13 | Agent-Workbench | Rebuilt the product surface as an Agent Workbench and extended backend support APIs. Changed `web/` from the previous front-end-owned state into Codex-owned workbench UI: workspace/upload/history pane, Agent Studio, PM playbook chips, responsive dynamic SVG route/evidence visuals, Evidence/Trace/Output Inspector, artifact shelf, upload/delete flows, and robust empty/error states. Added backend `GET /api/workspaces/{workspace_id}/dashboard` and optional `ChatRequest.playbook`, `artifact_mode`, `ui_context` fields that only add UI context to routing/prompts. Reusable local commands: `npm install lucide-react`; `npm run build`; `python -m pytest tests/test_agent_workbench_api.py -q`; `python -m compileall backend`; short TestClient `/api/chat` SSE smoke with `playbook/artifact_mode/ui_context`; Playwright viewport QA using explicit Chromium executable because project dependencies intentionally do not include Playwright. | Local build/test passed. Viewport QA covered 1440x960, 1280x820, 768x900, and 390x844 CSS px with `overflowX=false`, no console/http errors, six route SVG nodes, evidence SVG present, and readable panel widths. Mobile layout was adjusted so Agent Studio is first in the narrow-screen first viewport instead of showing only workspace metadata. Lightweight UI interaction QA passed: Inspector tabs `Evidence/Trace/Output`, three output cards, upload modal, file input, route nodes, no console/http errors. One full UI model-chain script hit the 120s tool ceiling, but backend logs showed the POST completed with final output; short local `/api/chat` SSE smoke returned `ready,user,route,plan,clarify` with zero errors. |
| 2026-06-13 | Agent-Workbench-Deploy | Loaded `数据产品化Agent/.dataforge-codex.env`, verified Azure account/subscription, and deployed both backend and web to the existing Azure environment. Backend remote build command pattern: `az acr build -r acrdataforgedev -t dataforge-backend:agent-workbench-20260613-1722 -f backend/Dockerfile .`. Web remote build command pattern from `web/` context: `az acr build -r acrdataforgedev -t dataforge-web:agent-workbench-20260613-1722 -f Dockerfile --build-arg VITE_API_BASE=https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io .`. Web ACR log streaming initially tripped Windows `gbk` output encoding after the run was queued; self-heal used UTF-8 shell output plus `az acr task show-run -r acrdataforgedev --run-id ch26`, which confirmed success. Updated apps with `az containerapp update` for `ca-dataforge-backend` and `ca-dataforge-web`. | ACR backend run `ch25` succeeded; image `acrdataforgedev.azurecr.io/dataforge-backend:agent-workbench-20260613-1722`, digest `sha256:b7400b03928b756c71c2af6eef3f853f74eaa8f82600f5af3828d9e3f2f91b70`. ACR web run `ch26` succeeded; image `acrdataforgedev.azurecr.io/dataforge-web:agent-workbench-20260613-1722`, digest `sha256:a8fc1151d4ce5592291a22ebfdb2340b24c3416d09e35ba8c3daeda6701fee3e`. Backend revision `ca-dataforge-backend--0000047` and web revision `ca-dataforge-web--0000027` are active, Healthy, Running, and at 100 percent traffic. Cloud `/api/health` returned Foundry/MCP/Speech/Blob all true, `/api/workspaces/demo-corpus/dashboard` returned 200, web endpoint returned 200, and cloud `/api/chat` SSE with new UI fields returned events `ready,user,route,plan,answer_delta,model_response,final` with zero errors. Anonymous Playwright to cloud web redirects to Microsoft login because the web app is auth-protected; deployed visual DOM requires an authenticated browser session. |
| 2026-06-13 | Batch11-P0-fix | Kept `web/` untouched. Fixed recording-visible Batch11 P0 surface issues: opportunity titles now derive from evidence instead of replaying/truncating user questions; `corpus_qa` renders consultant-style synthesized paragraphs plus numbered actions instead of per-hit formula lines; customer sanitizer removes internal English terms such as `generated data product` and `synthetic data`; feasibility final text now starts with action plan, recommendation, and 3-5 numbered steps before renderable score lines; feasibility artifact carries `recommendation` and `action_plan`. Rebuilt Foundry prompt agents after prompt update. Reusable commands: `python -m compileall backend ingest eval agents`; `python eval/calibration_gate.py --bad-rubric-smoke`; `python eval/batch11_p0_eval.py --skip-cloud-feasibility`; `python eval/batch11_p0_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Local and cloud Batch11 P0 fix evals passed. Cloud checks covered forbidden visible grep zero including `generated data product`/`synthetic data`, no `命中记录显示`/`类别集中在` templates, three different corpus questions with complete non-echo opportunity titles, `citations>=3`, feasibility `final.text` beginning with `**行动方案**`, >=3 numbered steps, score lines matching `名称 N/5：...`, artifact `recommendation/action_plan`, prompt-injection rejection, blind verdict event/artifact, and calibration gate pass. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch11-p0-fix-20260613-action-first` digest `sha256:2ae4623d37dd7f020e983a963e6beaaabc6b11b53bbeb8fa6f5254f5ae699ff1` to healthy revision `ca-dataforge-backend--0000046` with 100 percent traffic. Foundry agents rebuilt including `df-feasibility-analyst:5`; health dependencies Foundry/MCP/Speech/Blob all true. Cloud cleanup removed the passing eval workspace docs/blobs. |
| 2026-06-13 | Batch11-P0 | Kept `web/` untouched. Added explicit feasibility rubric and calibration gate, customer-visible enum/field sanitizer, natural Search hit titles and corpus QA wording, readable opportunity titles, blind/revised verdict contract with `blind_verdict` SSE, and prompt-injection/high-score guardrails. Rebuilt Foundry agents after prompt/rubric updates. Reusable commands: `python -m compileall backend ingest eval agents`; `python eval/calibration_gate.py --bad-rubric-smoke`; `python eval/batch11_p0_eval.py --skip-cloud-feasibility`; `python eval/batch11_p0_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Local and cloud Batch11 P0 evals passed all checks: forbidden customer-visible enum/raw-field grep was zero, citations had natural titles and at least 3 grounded references, blind verdict artifact/event carried `rubric_version=feasibility-rubric-v2026-06-13`, prompt-injection/high-score requests were visibly rejected, revised-verdict contract was validated, and calibration gate passed with Spearman 1.0, 0 inversions, and bad-rubric smoke failure as expected. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch11-p0-20260613-guard` digest `sha256:1d39419ab87f19a08e06c1821ad110586e30a4868bed13af208737b2d1db5ad0` to revision `ca-dataforge-backend--0000045`, healthy with 100 percent traffic. Foundry agents rebuilt including `df-feasibility-analyst:4` and `df-auditor:4`. Cloud cleanup deleted the passing eval workspace docs/blobs. |
| 2026-06-13 | Batch10 | Implemented customer-facing answer contract after keeping `web/` untouched. Added generic `customer_text` label/sanitization layer, structured clarify payload `{question, options, allow_multi, allow_freeform}`, stable `output_contract`, customer summaries/friendly labels on `GET /api/workspaces/{id}`, answer rendering that starts with direct judgment before evidence, raw field/path cleanup for body and citation snippets, and Search local/remote graceful fallback on `URLError`. Reusable commands: `python -m compileall backend ingest eval`; `python eval/batch10_answer_contract_eval.py`; `python eval/batch10_answer_contract_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Local and cloud Batch10 evals passed all checks: structured clarify had 5 Chinese options, `answer_delta` concatenation equaled `final.text`, repeated same question produced non-identical natural openings, CSV/JSON workspace summaries differed, no raw field keys (`home_branch`, `usage_score`, etc.) appeared in customer-facing answer/citation snippets, and final artifacts carried `output_contract.version=batch10.customer_text.v1`. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch10-20260613-answer-contract` digest `sha256:85d2f16270f008e3b151a02e4dfe0a477bb6d879f85cc0f6869a4e6b811b4775` to revision `ca-dataforge-backend--0000043`, active/healthy with 100 percent traffic. Health showed Foundry/Speech/Blob true; MCP health probe timed out and is not on the Batch10 path. Cloud cleanup deleted the passing eval workspaces and two residual workspaces from the first interrupted eval; each residual delete removed 4 Search docs and 4 blobs. |
| 2026-06-13 | Batch8-fix | Fixed cross-replica reference image reads after pulling latest `main` at `0abb17e`; `web/` remained untouched. `get_reference_image_content()` now uses the deterministic Blob path `workspaces/{workspace_id}/reference_images/{safe_filename}` before any metadata fallback, and `workspace_reference_images()` can list reference assets from the Blob registry when local workspace files are absent. Reusable commands: `python -m compileall backend ingest eval`; `python eval/batch8_reference_image_eval.py`; `python eval/batch8_reference_image_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io --full-produce`. | Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch8-fix-20260613-blob-reference` digest `sha256:5331df1d5344db8bb32222d5dcca6906ab787356e02b44887dd210700c4b1172` to revision `ca-dataforge-backend--0000042`, active with 2 replicas and 100 percent traffic. Cloud eval passed: proxy GET loop returned five `200` PNG reads at 993 bytes; primary blob_name `workspaces/upload-batch8-logo-workspace-1781322756-20260613035241/reference_images/batch8-logo.png`; `/api/generate-image` returned `mode=gpt-image-2-edit`, `reference_image_count=1`; `/api/produce` returned `mode=gpt-image-2-edit`, `reference_image_count=1`; alternate logo also used edit mode and produced a different SHA-256; no-reference generation returned `mode=gpt-image-2`, `reference_image_count=0`. |
| 2026-06-12 | Batch8 | Added reference image assets for upload workspaces and gpt-image-2 image-edit generation. `/api/upload` now accepts png/jpg/jpeg/webp files with `asset_role`, stores them under the workspace as `reference_images`, exposes them through detail/proxy APIs, and producer/image generation passes workspace reference image URLs into `images/edits` before falling back to generations/deterministic PNG. Reusable command: `python eval/batch8_reference_image_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io --full-produce`. | Cloud revision `ca-dataforge-backend--0000041` passed. Uploaded logo workspace `upload-batch8-logo-workspace-1781259024-20260612101027`; `GET /api/workspaces/{id}` returned `reference_images[0].role=logo`; proxy returned PNG bytes; `/api/generate-image` returned `mode=gpt-image-2-edit`, 1,159,577 bytes, `reference_image_count=1`; `/api/produce` concept image also returned `mode=gpt-image-2-edit`, 1,320,208 bytes. Cleanup deleted 3 workspace blobs. |
| 2026-06-12 | Batch9 | Polished recording-visible answer behavior: opportunity labels are de-duplicated/readable, coordinator prompt and fallback route favor grounded answers for advice/plan requests, corpus_qa synthesizes natural-language recommendations instead of raw record dumps, citations are per-hit markers, and feasibility next steps use retrieved evidence signals. Reusable command: `python eval/batch9_polish_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Cloud revision `ca-dataforge-backend--0000041` passed. Three repeated “活动推广怎么做” queries all routed `corpus_qa`, `clarify_counts=[0,0,0]`, `citation_counts=[8,8,8]`; visible answer used synthesized sections and no `raw_docs/` leak. Title normalization converted `banana-climbing-shenzhen-banana-climbing-shenzhen-banana-climbing-shenzh` to `Banana Climbing Shenzhen 做一个活动让攀岩馆推广方案`. Cleanup removed 56 Search docs and 4 blobs. |
| 2026-06-12 | Batch8/9-Deploy | Built and deployed backend image `acrdataforgedev.azurecr.io/dataforge-backend:batch8-9-20260612-reference-polish` after pulling latest `main` at `368934c`; `web/` remained untouched. | Image digest `sha256:359eab20c05215294761a153eb0ec28f1235d5dbaaf872541e648e0eabcb8f6d`; Container App revision `ca-dataforge-backend--0000041` is `Healthy`, active, and serving 100 percent traffic. `/api/health` returned Foundry/MCP/Speech/Blob all true. |
| 2026-06-12 | Batch6-WP-R4-fix | Fixed `/api/upload` append semantics and description persistence after pulling latest `main` at `800931e`; `web/` remained untouched. Reusable command: `python eval/batch6_upload_append_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Cloud revision `ca-dataforge-backend--0000040` passed: first upload returned `upload-batch6-two-file-append-workspace-20260612090958`; second upload with the same `workspace_id` returned the same id; `doc_count>=2`, `documents` contained both files, `description` persisted, search changed after append, aggregate profile gained the second file fields, and single-file upload stayed compatible. |
| 2026-06-12 | Batch7-URGENT | Added generic content-level indexing for uploads in addition to profile indexing. CSV/JSON/Excel records become Search docs; Markdown/text chunks become Search docs; JSON top-level objects with multiple arrays index every array record. Reusable command: `python eval/batch7_content_index_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. | Cloud revision `ca-dataforge-backend--0000040` passed. Uploading `D:\Demo\banana_climbing_shenzhen.json` produced `indexed_count=56` and search returned 10 hits with real `document_type=json` records including activity/sponsor/member/peripheral/hand-care snippets, not just a profile. CSV and Excel uploads returned row-level content hits; Excel hits carried `sheet`/`row`. `/api/chat` for the banana workspace returned `chat_citations_count=1` citing `raw_docs/banana_climbing_shenzhen.json`. Delete cleanup removed 56, 3, and 3 Search docs for the JSON/CSV/Excel test workspaces. |
| 2026-06-12 | Batch7-Deploy | Built and deployed backend image `acrdataforgedev.azurecr.io/dataforge-backend:batch7-20260612-content-index-citations`; `.dockerignore` was tightened to keep ACR build context focused; `web/` remained untouched. | Container App revision `ca-dataforge-backend--0000040` is healthy with 100 percent traffic. `/api/health` returned all dependencies true: Foundry, MCP, Speech, and Blob. |
| 2026-06-12 | Batch5-Redis | Provisioned Azure Managed Redis for the test environment after Azure rejected new legacy Azure Cache for Redis creation as retiring. Commands: `az extension add --name redisenterprise`; `az redisenterprise create --name redis-dataforge-dev --resource-group rg-dataforge-dev --location eastus2 --sku Balanced_B0`; `az redisenterprise database create --cluster-name redis-dataforge-dev --name default --client-protocol Encrypted --access-keys AuthenticationEnabled`; set Container App secret `redis-url` and env `DF_REDIS_URL=secretref:redis-url`. | Redis endpoint `redis-dataforge-dev.eastus2.redis.azure.net:10000`; local probe over TLS returned set/get ok. Deployed backend revision later uses the secret, with Redis failures handled as direct compute plus explicit cache status. |
| 2026-06-12 | Batch5-R1/R2 | Implemented LLM coordinator routing and clean structured final output; reusable validation commands were cloud/local `/api/chat` SSE scripts against smalltalk, clarify, full feasibility, and followup-edit cases. | Cloud route checks on backend revision `ca-dataforge-backend--0000037`: `hello` routed `smalltalk_or_meta`, experts `[]`, agent `df-coordinator` only; `modify format` without prior context routed `clarify_needed`, experts `[]`; feasibility query routed `feasibility_analysis` with corpus/feasibility/auditor, final markdown sections present, `citations_count=4`, no raw `raw_docs` ref leak, and `answer_delta` concatenation equaled `final.text`. Followup conversation `batch5-followup-fix-bd07428e` second turn `make it English` routed `followup_edit`, experts `[]`, no feasibility/market model calls, final English markdown, and no raw JSON shell. |
| 2026-06-12 | Batch5-R3 | Added Redis cache layer for feasibility analysis with key fields `workspace_id`, chunk fingerprint, prompt version, retrieval mode, and query hash. Reusable local command pattern: run the same demo-corpus feasibility `/api/chat` twice with `DF_REDIS_URL` set, then run a different uploaded workspace query. | Local miss/store run took 67.799s, second identical run hit cache in 2.484s with same result hash. Key sample: `dataforge:analysis:v1:workspace=demo-corpus:fingerprint=91be2fe3028884292d982063:prompt=df-feasibility-analyst:batch5-r1:retrieval=keyword:query=93ff47610d321490`. Different workspace `upload-cn-5e0d03538a-20260612065355` produced a miss with a different fingerprint/result. Cloud cache hit on demo-corpus took 9.409s with cache status `hit`, Redis elapsed 20ms, key sample `dataforge:analysis:v1:workspace=demo-corpus:fingerprint=fbbadac727e1a9f79ec94941:prompt=df-feasibility-analyst:batch5-r1:retrieval=hybrid:query=e273cd49eb8b67f1`. Redis unconfigured/unavailable path direct-computes without `event:error`. |
| 2026-06-12 | Batch5-R4 | Extended `POST /api/upload` to accept optional `name`, `description`, and multiple `file` parts into one workspace; updated `GET /api/workspaces` and `GET /api/workspaces/{id}` to return description and document metadata. Reusable command pattern: multipart POST three files with `name` and `description`, then read list and detail APIs. | Cloud upload returned workspace `upload-cn-5e0d03538a-20260612071456`, `indexed_count=3`, `documents=3`, display name from the user field, and description from the form field. List and detail APIs returned the same name, description, and three-document list. |
| 2026-06-12 | Batch5-Deploy | Built and deployed backend image `acrdataforgedev.azurecr.io/dataforge-backend:batch5-20260612-json-followup-fix`; `web/` remained untouched. | Container App revision `ca-dataforge-backend--0000037` is healthy/running at 100 percent traffic. Current backend URL remains `https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io`. |
| 2026-06-11 | WP-Onboarding | Implemented upload onboarding backend after pulling latest `main`; `web/` remained untouched. | Added CSV/JSON/Excel/MD profile adapters, profile-only Search indexing, `POST /api/upload`, `GET /api/workspaces`, and coordinator LLM Chinese clarify guidance. `python eval/onboarding_upload_eval.py` passed all redlines: three formats searchable, conclusions changed across data, and three `你好` replies varied with `mode=coordinator_llm`. Deployed `dataforge-backend:onboarding-20260611-172939`; cloud health, upload/search/workspaces smoke, and cloud `你好` guidance passed. |
| 2026-06-11 | WP-R3-hotfix | Fixed evidence verification blocker after pulling `main` at `9ed6beb`; `web/` remained untouched. | `_verify_evidence` now normalizes source/chunk refs, accepts reordered `source#sheet#row#chunk` citations, prunes invalid evidence instead of throwing, and 5 demo-corpus product-feasibility runs produced `total_errors=0` in `docs/ref_verification_regression.json`. Deployed `dataforge-backend:ref-verify-20260611-154007`; cloud health passed and one cloud `/api/chat` smoke returned `error_count=0`, `mode=foundry_agent_service`, and 5 dimensions. |
| 2026-06-11 | U3/U4/U6/U7/U8/U5 | Pulled `main` at `eb7021f` before work and kept `web/` untouched. | Implemented grounded `answer_delta` streaming with final parity, Blob-backed upload workspace persistence and delete, generic cross-signal profiling, verified Foundry-native market web search, AI Search hybrid vector retrieval, and optional `/api/produce`. |
| 2026-06-11 | U3 | Verified streaming parity and health during answer stream. | Direct orchestration produced 539 `answer_delta` events, `delta_chars=1101`, `final_chars=1101`, `delta_equals_final=true`, `errors=[]`. Local server long-stream check after first `answer_delta` saw 8 `/api/health` calls with max 0.02s and no errors. |
| 2026-06-11 | U4 | Verified CSV/JSON/Excel upload, Blob persistence, and deletion. | CSV/JSON/Excel each returned `indexed_count=1` and Search count 1; delete returned `deleted_docs=1`, `deleted_blobs=3`, and post-delete Search count 0 for each. Simulated redeploy by deleting local cache for `upload-u4-persist-eval-20260611122012`; `GET /api/workspaces` still listed it from Blob and Search count remained 1 before delete. |
| 2026-06-11 | U6 | Verified generic cross-signal profiler on two different datasets. | Dataset A surfaced `revenue 按 segment` and downward date trend; dataset B surfaced `cost 按 plan` and upward date trend. Date fields are excluded from category grouping and only used for trend detection. |
| 2026-06-11 | U7 | Verified Foundry-native web search before using it in market step. | Probe confirmed `web_search_preview` works on the `gpt-5.1` deployment; direct market research returned 4 sources and all external findings were `market_inferred`. End-to-end market trace emitted `foundry_native_web_search`, `mode=foundry_native_web_search_preview`, `count=4`, `source_count=4`, `verification_tool=web_search_preview`, and no errors. |
| 2026-06-11 | U8 | Migrated AI Search schema and verified hybrid retrieval. | Online schema update added `content_vector`; rebuilt demo/excel docs with 1536-dim `text-embedding-3-small` vectors. Semantic query `customers are worried about nightly processing expense...` hit nightly batch cost evidence with `retrieval_mode=hybrid`; keyword query also kept returning hits. |
| 2026-06-11 | U5 | Added optional producer-only endpoint. | `POST /api/produce` accepts existing feasibility/corpus/market payload and runs producer tools without rerunning coordinator, corpus, feasibility, market, or auditor. |
| 2026-06-11 | Deploy | Built and deployed backend image `dataforge-backend:u3-u8-20260611-2046`. | Container App revision `ca-dataforge-backend--0000023` succeeded with `AZURE_OPENAI_API_KEY`, `AZURE_STORAGE_KEY`, `DF_STORAGE_ACCOUNT`, `OPENAI_ENDPOINT`, and embedding envs. Cloud health passed; demo semantic Search returned `retrieval_mode=hybrid`; cloud upload `upload-cloud-u4-smoke-retry-20260611125234` returned `indexed_count=1`, Search hit on first retry with `hybrid`, and delete removed `deleted_docs=1`, `deleted_blobs=3`. Cloud `/api/chat` trace reached `foundry_native_web_search` in 52.85s with 4 sources, `mode=foundry_native_web_search_preview`, and no errors. |
| 2026-06-12 | Batch4-S1 | Pulled `main` at `b78139e`, kept `web/` untouched, and deployed backend image `acrdataforgedev.azurecr.io/dataforge-backend:batch4-20260612-health-mcp-timeout` to revision `ca-dataforge-backend--0000034`. | Long-stream stability changes: SSE heartbeats/progress capped at 8s, answer deltas coalesced, answer writer capped, market lookup overlapped/cached, producer artifacts run in parallel, and audit revision loop only triggers on blocking evidence/safety failures. True Chromium/Playwright browser fetch ran 5 cloud `/api/chat` streams with `failures=0`, no network errors, no `event:error`, and `answer_delta` concat equal to `final.text`. Durations were 40.203s, 40.715s, 36.755s, 42.744s, 37.243s; max chunk gaps were 7.990s, 7.989s, 8.018s, 8.115s, 8.058s. ACA environment `cae-dataforge-dev` has no Premium ingress config; observed/current timeout remains the default 240s. |
| 2026-06-12 | Batch4-S2 | Replaced env-only health with true dependency probes and verified cloud `/api/health`. | `/api/health` now probes Foundry, MCP, Azure Speech token issue, and Blob container properties with short timeout and cache while preserving bool `dependencies`. Cloud checks on revision `0000034`: cold 2.213s and cached 0.717s/0.453s with `foundry=true`, `mcp=true`, `speech=true`, `blob=true`; local negative test with a bad Speech key returned `speech=false` and HTTP 401 detail before restoring the real key. |
| 2026-06-12 | Batch4-S3 | Added Blob-backed conversation persistence and conversation APIs. | Added `GET /api/conversations`, `GET /api/conversations/{conversation_id}`, and `conversation_id` continuation in `POST /api/chat`. Cloud continuity smoke `batch4-continuity-1781241492022` ran two turns in the same conversation: turn durations 40.395s and 47.236s, both final with no errors and stream/final parity. Detail API returned 4 messages with roles `user,assistant,user,assistant`, `turn_count=2`, `workspace_id=demo-corpus`; list API included the conversation. The second assistant reply explicitly continued from the sales-operations idea and switched to customer-support automation. |
| 2026-06-10 | WP0 | Initialized independent repository under `dataforge-agent`. | Started. |
| 2026-06-10 | WP0 | Checked required repository paths. | Passed. |
| 2026-06-10 | WP1 | Wrote Terraform modules for RG, monitoring, storage, search, speech, ACR, and Container Apps. | Pending validation. |
| 2026-06-10 | WP1 | First Terraform apply. | Partial success; fixed Container Apps Log Analytics ID and moved Search to `eastus` after eastus2 capacity error. |
| 2026-06-10 | WP1 | Terraform apply and output key check. | Passed; Search created in `eastus`, all other application resources in `eastus2`. |
| 2026-06-10 | WP2 | Started demo corpus and RAG indexing implementation. | In progress. |
| 2026-06-10 | WP2 | Ran `python ingest\\build_index.py`, `search_smoke.py`, and `feasibility_smoke.py`. | Passed. |
| 2026-06-10 | WP3 | Started backend tool service implementation. | In progress. |
| 2026-06-10 | WP3 | Built and deployed backend Container App. | Passed cloud health and search smoke. |
| 2026-06-10 | WP4 | Started MCP market server implementation. | In progress. |
| 2026-06-10 | WP4 | Built and deployed MCP market Container App. | Passed health, HTTP tool, and MCP JSON-RPC smoke. |
| 2026-06-10 | WP5 | Started Azure Speech narration implementation. | In progress. |
| 2026-06-10 | WP5 | Injected Speech/Search secrets as Container App secrets and validated narration. | Passed with Azure Speech WAV artifact. |
| 2026-06-10 | WP6 | Started Foundry agent definitions. | In progress. |
| 2026-06-10 | WP6 | Created six Foundry prompt-agent versions and ran verification. | Passed `foundry_get`, strict function schema check, and six-agent `responses_smoke`. |
| 2026-06-10 | WP7 | Started coordinator-driven orchestration implementation. | In progress. |
| 2026-06-10 | WP7 | Ran local and cloud `/api/chat` orchestration smoke tests. | Passed product/corpus routing, clarify short-circuit, MCP trace, and auditor revise/pass path. |
| 2026-06-10 | WP8 | Wired producer tools and ran full-package cloud smoke. | Passed with downloadable PDF, deterministic concept PNG, and Azure Speech WAV. |
| 2026-06-10 | WP9 | Started three-column trace UI from generated concept. | In progress. |
| 2026-06-10 | WP9 | Built React/Vite UI and ran Playwright visual/workflow smoke. | Passed: no console errors, 3 artifact links, 20 trace events, 6 agents visible, desktop/mobile screenshots saved. |
| 2026-06-10 | WP10 | Added router evals and structured trace logging, then deployed backend. | Passed 10 routing cases at 100% accuracy/consistency; App Insights traces returned `dataforge_trace` events. |
| 2026-06-10 | WP11 | Started end-to-end demo script and runbook. | In progress. |
| 2026-06-10 | WP11 | Ran `scripts/demo_e2e.py` against the cloud backend. | Passed all demo checks: clarify, 6 agents, MCP, full package artifacts, Azure Speech, auditor revise/pass, honest no. |
| 2026-06-11 | WP-A | Replaced scripted feasibility/auditor conclusions with Foundry Responses calls and schema validation. | Passed `python eval/next_stage_wp_a_eval.py`; result saved to `docs/wp_a_next_stage_eval.json`. |
| 2026-06-11 | WP-B | Reworked React UI for Chinese local-first workflow and wired previously dead controls. | Passed `npm run build` and Browser interaction checks; committed UI screenshots and `docs/wp_b_ui_eval.json`. |
| 2026-06-11 | WP-C | Added Excel adapter, sample spreadsheets, mixed workspace manifests, Search schema sheet/row fields, and Chinese analyzer. | Passed `python eval/next_stage_wp_c_eval.py`; cloud index returned Excel `sheet`/`row` hits and Foundry produced an excel-corpus feasibility report. |
| 2026-06-11 | WP-C | Rebuilt and deployed backend Container App image `dataforge-backend:next-20260611-2315`. | Passed cloud `/api/health` and cloud `/api/chat` for `excel-corpus` with 2 model responses, final summary, and no errors. |
| 2026-06-11 | WP-R1/R2 | Implemented post-review fixes: empty evidence deterministic downgrade, CJK-friendly local search without fixed Chinese dictionary, markdown language detection, and threadpool wrapping for blocking calls. | Local `python eval/revise_order_eval.py` passed; first frame latency 0.001s; Chinese Excel hit included `sheet=QualitySignals,row=2`; zero-hit query produced no `event:error`. |
| 2026-06-11 | WP-R1/R2 | Deployed `dataforge-backend:revise-20260611-r1r2b`, changed health/tool endpoints to avoid blocking request handling, and set backend Container App to 0.5 CPU, 1Gi memory, min replica 1. | Cloud `python eval/revise_order_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io` passed; first frame 0.00005s; warm in-stream health 0.425s; public new-connection health baseline recorded as 1.759s. |
| 2026-06-11 | WP-R3 | Merged PR #1 frontend into `main`, then kept `web/` frozen while upgrading backend agent execution to Foundry prompt-agent `agent_reference` tool loops. | `df-feasibility-analyst` and `df-auditor` produced `mode=foundry_agent_service`, `response_id`, usage, and autonomous `search_pack_context` tool calls in SSE/model metadata. |
| 2026-06-11 | WP-R3 | Added `user-excel-corpus` external Excel workspace using `DF_USER_EXCEL_PATH`; ingested the local Chinese workbook into Azure Search without committing the source Excel file. | Uploaded 189 rows; Chinese search returned `source_file=external/user-chinese-workbook.xlsx`, concrete `sheet` and `row`; `python eval/wp_r3_agent_service_eval.py` passed and saved `docs/wp_r3_agent_service_eval.json`. |
| 2026-06-11 | WP-R3 | Fixed final text sentence-end cleanup for empty-evidence and normal gap summaries. | `python eval/revise_order_eval.py` passed; empty-evidence final text no longer contains `。。`. |
| 2026-06-11 | WP-R3 | Built and deployed backend image `dataforge-backend:wp-r3-20260611-agent-service`. | Cloud `/api/health` passed; cloud `/api/chat` for `user-excel-corpus` emitted autonomous feasibility/auditor `search_pack_context` calls, both model responses used `mode=foundry_agent_service`, verdict was `conditional`, first hit included `sheet`/`row`, and `docs/wp_r3_cloud_smoke.json` passed. |
