# DataForge FinOps Demo Integrity Acceptance

Date: 2026-08-06
Status: local candidate accepted; Azure deployment and production traffic switch not performed

## Evaluated range and boundary

- Product baseline: `480a5a9`
- Completed Task 1-6 product head: `764e992`
- Product implementation range: `480a5a9..764e992`
- Task 7 acceptance changes: candidate assertions, stale browser-contract corrections, customer-facing evidence-status localization, and this report. They are committed together as `test(finops): gate the complete demo integrity flow`.
- Authentication, Easy Auth, Entra tenant/workspace authorization, cross-tenant restrictions, and governance execution gates were not changed.
- `DF_FINOPS_ACTIONS_ENABLED` remains disabled. Risk scans and Operations AI remain advisory/read-only.

## Acceptance contract

`backend/finops/candidate_acceptance.py` now fails closed unless the candidate proves all of the following:

1. ROI value bridge uses `monthly benefit + negative operating cost = monthly net benefit`; the metric card keeps cost positive.
2. All four ROI maturity stages expose request-level `req_*` evidence and each reference can be opened through the existing request-detail API.
3. Risk decisions contain at least six distinct policy evidence sets and at least six distinct representative request sets.
4. Request status, cache status, pricing status, and entry-governance string signals are present in their closed service contract for customer localization.
5. Operations AI returns exactly the evidence references selected for the current item and returns friendly evidence labels.
6. `df-finops-analyst` and `df-roi-analyst` are both assigned Terra as the primary route and `analysis` as fallback.
7. Production governance actions remain disabled.

## Fresh gate evidence

All commands were run from `C:\Users\12140\Documents\Agent-Demo-project-worktrees\codex-finops-prod-20260805` unless a `web` working directory is stated.

| Gate | Command | Fresh result | Log |
|---|---|---:|---|
| Candidate contract | `python -m pytest -q tests/test_finops_candidate_acceptance.py` | 11 passed | `.superpowers/sdd/final-gate-logs/python-candidate-final.txt` |
| Python full suite | `python -m pytest -q` | 1755 passed, 1 skipped, 1 existing experimental warning | `.superpowers/sdd/final-gate-logs/python-full.txt` |
| Node full suite | `node --test` | 290 passed | `.superpowers/sdd/final-gate-logs/node-full-after-visual-fix.txt` |
| Vite production build | `npm run build` | 1793 modules; build succeeded | `.superpowers/sdd/final-gate-logs/vite-build-after-visual-fix.txt` |
| Playwright full suite | `DF_PLAYWRIGHT_PORT=5302; npx playwright test` | 60 passed on a verified-free port with a freshly started preview | `.superpowers/sdd/final-gate-logs/playwright-full-after-visual-fix-port-5302.txt` |
| Whitespace/conflict markers | `git diff --check` and `git diff --check 480a5a9..HEAD` | clean before this report and at final staging | `.superpowers/sdd/final-gate-logs/git-diff-check-working.txt`, `.superpowers/sdd/final-gate-logs/git-diff-check-branch.txt` |

The first full Playwright run correctly exposed two stale acceptance assertions rather than product failures: the approved demo ROI scenario now carries `$100` monthly model cost instead of the old `$0.0269`, and the customer-facing latency unit is now `毫秒` instead of raw `ms`. Both assertions were updated to the current contract, passed a focused two-test rerun, and then passed in the final 60-test full run.

The `$100` value is an approved ROI scenario input used with implementation and fixed operating costs. It is not the request-level `$0.0269` estimated AI usage cost shown in the operations overview, and neither value is an Azure invoice claim.

## Visual inspection

The following screenshots were freshly generated by the isolated-port browser run and reviewed at original resolution:

- Overview: `output/playwright/operations-management-overview-desktop.png`
- Cost analysis: `output/playwright/operations-cost-analysis-desktop.png`
- ROI desktop/mobile: `output/playwright/operations-roi-desktop.png`, `output/playwright/operations-roi-mobile.png`
- Risk full desktop/mobile: `output/playwright/operations-management-desktop.png`, `output/playwright/operations-risk-mobile.png`
- Risk decision stage desktop/mobile: `output/playwright/finops-risk-stage-desktop.png`, `output/playwright/finops-risk-stage-mobile.png`
- Item-specific evidence: `output/playwright/risk-latency-evidence-desktop.png`, `output/playwright/risk-cache-evidence-desktop.png`
- Generic request evidence drawer: `output/playwright/finops-evidence-desktop.png`, `output/playwright/finops-evidence-mobile.png`
- Operations AI mobile popover: `output/playwright/operations-management-mobile.png`
- Model and price settings: `output/playwright/operations-model-settings-desktop.png`

The review checked desktop and 390 px layout, value-bridge operators, data-proportional charts, drawer and popover boundaries, distinct latency/cache evidence, post-scan priority refresh, ROI stage drill-down, and settings reachability. No blocking overlap, clipping, horizontal page overflow, repeated item evidence, stale post-scan priority, or incorrect ROI operator remained.

Visual review did find raw `succeeded` and `recorded` enum values in the request evidence drawer and raw timeline states. They are now mapped through a closed customer label set (`调用成功`, `已记录`, `已观测`, and other bounded states); unknown values fail closed to `未记录`. The browser acceptance asserts that `succeeded` and `recorded` no longer appear in the drawer, and the refreshed screenshot was re-inspected.

## Secret and artifact scan

A bounded scan covered the tracked implementation diff `480a5a9..764e992`, the Task 7 files intended for commit, and high-confidence secret patterns across tracked files. It reported:

- private-key headers: 0
- GitHub PAT/token patterns: 0
- literal bearer credentials: 0
- Azure connection-string secrets: 0
- generic literal API key/client secret/password assignments in the diff: 0
- tracked generated output/test-result paths: 0

The scan summary is stored at `.superpowers/sdd/final-gate-logs/secret-artifact-scan.txt`; it contains counts only and no credential values. `.superpowers/sdd`, `web/test-results`, screenshots, and `workspaces/ws-*` runtime directories are deliberately excluded from the commit.

## Known non-blocking advisory

Vite reports the existing main bundle chunk at approximately 577 kB, above its 500 kB advisory threshold. The build succeeds, FinOps is already split into its own chunk, and this work did not introduce a new warning class. Further general-shell code splitting remains a separate performance improvement.

## Candidate-only release checklist

No step below was executed in this task:

1. Build immutable backend and web images from the reviewed commit.
2. Deploy backend and web as zero-traffic candidate revisions; do not alter Easy Auth or Entra configuration.
3. Keep `DF_FINOPS_ACTIONS_ENABLED=0` and all external/action gates at their accepted disabled values.
4. Run health checks, the candidate acceptance command, and authenticated desktop/mobile smoke tests against the zero-traffic revisions.
5. Confirm the demo workspace returns non-zero request, Token, estimated-cost, cache, risk, ROI, and request-detail evidence without cross-workspace access.
6. Reconfirm Terra-primary/analysis-fallback assignments for the two operations analysts and verify no provider secret is returned to the browser.
7. Record immutable revision names and rollback targets.
8. Obtain explicit user approval before any production traffic change.

Azure deployment, merge, push, and production traffic switching were not performed as part of this acceptance task.
