# DataForge Build Progress

| WP | Scope | Status | Timestamp | Acceptance |
|---|---|---|---|---|
| Batch11-P0-fix | Recording-visible Batch11 surface fixes: evidence-based titles, synthesized corpus QA, action-first feasibility | done | 2026-06-13 | `web/` remained untouched. Opportunity titles now come from retrieved evidence rather than replaying/truncating user questions; `corpus_qa` answers are synthesized consultant-style paragraphs with numbered actions instead of per-hit formula bullets; customer text sanitizer removes internal English terms including `generated data product` and `synthetic data`; feasibility answers start with `**行动方案**`, one-line recommendation, and numbered steps before renderable `名称 N/5：...` score lines; structured artifacts include `recommendation` and `action_plan`. Local `python -m compileall backend ingest eval agents`, `python eval/calibration_gate.py --bad-rubric-smoke`, and `python eval/batch11_p0_eval.py --skip-cloud-feasibility` passed. Cloud `python eval/batch11_p0_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io` passed: three different corpus questions had complete non-echo titles, no record-template phrases, forbidden grep was zero, `citations>=3`, action-first feasibility and artifact action plan were present, prompt-injection rejection and blind verdict remained valid. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch11-p0-fix-20260613-action-first` digest `sha256:2ae4623d37dd7f020e983a963e6beaaabc6b11b53bbeb8fa6f5254f5ae699ff1` to healthy revision `ca-dataforge-backend--0000046` with 100 percent traffic; Foundry `df-feasibility-analyst:5` rebuilt and `/api/health` reported Foundry/MCP/Speech/Blob true. |
| Batch11-P0 | Customer-visible polish, explicit feasibility rubric/calibration, and blind/revised verdict guards | done | 2026-06-13 | `web/` remained untouched. Added a versioned feasibility rubric (`feasibility-rubric-v2026-06-13`) with weighted dimensions, pre/post audit guardrails, blind verdict snapshot, revised verdict contract, and calibration gate. Customer-facing output now hides raw enum/field/internal labels, uses natural Chinese labels/titles, and rejects user attempts to force high scores or preset conclusions. Local checks passed: `python -m compileall backend ingest eval agents`, `python eval/calibration_gate.py --bad-rubric-smoke`, and `python eval/batch11_p0_eval.py --skip-cloud-feasibility`. Cloud `python eval/batch11_p0_eval.py --api-base https://ca-dataforge-backend.thankfultree-c0fc8321.eastus2.azurecontainerapps.io` passed: forbidden visible-token grep was zero, citations were grounded/natural, `blind_verdict` SSE/artifact was present, prompt injection was rejected, revised contract was validated, and cleanup removed eval workspace Search docs/blobs. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch11-p0-20260613-guard` digest `sha256:1d39419ab87f19a08e06c1821ad110586e30a4868bed13af208737b2d1db5ad0` to healthy revision `ca-dataforge-backend--0000045` with 100 percent traffic; Foundry agents rebuilt including `df-feasibility-analyst:4` and `df-auditor:4`. |
| Batch10 | Customer-facing answer contract, structured clarify, and workspace summaries | done | 2026-06-13 | `web/` remained untouched. Backend now hides raw English data field keys/schema refs from customer-facing clarify/corpus_qa/feasibility text and citation snippets through a generic field-label sanitizer; `GET /api/workspaces/{id}` returns `customer_summary` plus per-column `friendly_label`; clarify emits `clarify.question`, 2-5 Chinese `clarify.options[]`, `allow_multi=true`, and `allow_freeform=true`; final responses include `output_contract.version=batch10.customer_text.v1`, `[n]` citation markers, and direct first-paragraph judgment before evidence. Local and cloud `python eval/batch10_answer_contract_eval.py` passed: CSV/JSON summaries changed by dataset, same question repeated 3 times was not identical, `answer_delta` equaled `final.text`, no raw fields such as `home_branch` or `usage_score` appeared in body/citation snippets, and cloud test workspaces were deleted. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch10-20260613-answer-contract` digest `sha256:85d2f16270f008e3b151a02e4dfe0a477bb6d879f85cc0f6869a4e6b811b4775` to healthy revision `ca-dataforge-backend--0000043` with 100 percent traffic. |
| Batch8-fix | Blob-backed reference image reads across replicas | done | 2026-06-13 | `get_reference_image_content()` now reads reference image bytes directly from `workspaces/{workspace_id}/reference_images/{safe_filename}` when local replica state is absent, with Blob workspace metadata/registry as fallback; `workspace_reference_images()` lists reference assets from the Blob registry so `/api/produce` sees uploaded logos on any replica. Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch8-fix-20260613-blob-reference` digest `sha256:5331df1d5344db8bb32222d5dcca6906ab787356e02b44887dd210700c4b1172` to `ca-dataforge-backend--0000042`. Cloud `python eval/batch8_reference_image_eval.py --api-base ... --full-produce` passed: five proxy GETs returned 200 PNG, `/api/generate-image` and `/api/produce` both returned `mode=gpt-image-2-edit` with `reference_image_count=1`, alternate logo changed the concept image hash, and no-reference generation stayed `mode=gpt-image-2` with `reference_image_count=0`. `web/` remained untouched. |
| Batch8 | Reference image upload and gpt-image-2 edits | done | 2026-06-12 | `POST /api/upload` recognizes png/jpg/jpeg/webp as reference assets, stores them under the workspace, supports `asset_role`, and `GET /api/workspaces/{id}` returns `reference_images`. Producer/image generation now sends workspace reference images to `images/edits` and records `mode=gpt-image-2-edit`, with generations and deterministic fallback preserved. Cloud `python eval/batch8_reference_image_eval.py --api-base ... --full-produce` passed on revision `ca-dataforge-backend--0000041`: logo asset registered, proxy returned PNG, `/api/generate-image` and `/api/produce` both returned `mode=gpt-image-2-edit`, and cleanup deleted the reference blobs. |
| Batch9 | Recording polish for titles, routing, corpus_qa, next steps | done | 2026-06-12 | Opportunity labels are de-duplicated/readable; coordinator prompt and fallback routing now prefer grounded answers for advice/plan requests when workspace evidence exists; corpus_qa returns synthesized natural language with per-hit citations instead of raw record dumps; feasibility next steps are evidence-driven. Cloud `python eval/batch9_polish_eval.py --api-base ...` passed on revision `ca-dataforge-backend--0000041`: three repeated plan questions produced zero clarify events, all routed `corpus_qa`, each returned 8 citations, answer text had no `raw_docs/` leak, and the repeated banana opportunity id normalized to `Banana Climbing Shenzhen 做一个活动让攀岩馆推广方案`. |
| Batch8/9-Deploy | Backend rollout for reference images and polish | done | 2026-06-12 | Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch8-9-20260612-reference-polish` digest `sha256:359eab20c05215294761a153eb0ec28f1235d5dbaaf872541e648e0eabcb8f6d` to `ca-dataforge-backend--0000041`; revision is healthy with 100 percent traffic and `/api/health` reports Foundry/MCP/Speech/Blob true. `web/` remained untouched. |
| Batch6-WP-R4-fix | Upload append to existing workspace plus persistent description/documents | done | 2026-06-12 | `POST /api/upload` now appends new files to the supplied `workspace_id` instead of creating a new workspace; description, documents, aggregate profile, Blob registry, and local metadata are rewritten consistently. Cloud `python eval/batch6_upload_append_eval.py --api-base ...` passed on revision `ca-dataforge-backend--0000040`: same workspace id, `doc_count>=2`, both files in `documents`, description persisted, search/profile changed after append, and single-file upload remained compatible. |
| Batch7-URGENT | Upload content-level indexing for JSON/CSV/Excel/Markdown | done | 2026-06-12 | Uploads now index both profile summaries and real content records/chunks. JSON indexes all top-level array records, CSV/Excel index each row, Markdown/text indexes chunks; Search docs align with existing schema and deletion removes all workspace docs. Cloud `python eval/batch7_content_index_eval.py --api-base ...` passed on revision `ca-dataforge-backend--0000040`: banana JSON uploaded with `indexed_count=56`, search returned 10 real `json` records, CSV and Excel content were searchable with Excel `sheet`/`row`, and `/api/chat` returned citation(s) against `raw_docs/banana_climbing_shenzhen.json`. |
| Batch7-Deploy | Backend rollout for upload content indexing | done | 2026-06-12 | Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch7-20260612-content-index-citations` to revision `ca-dataforge-backend--0000040`; health returned Foundry/MCP/Speech/Blob all true; `web/` remained untouched. |
| Batch5-R1 | LLM coordinator intent routing and differential agent scheduling | done | 2026-06-12 | Coordinator now routes with an LLM schema into `feasibility_analysis`, `followup_edit`, `smalltalk_or_meta`, or `clarify_needed`, and emits route reason plus scheduled agents. Cloud revision `ca-dataforge-backend--0000037`: `hello` used only `df-coordinator`; `modify format` without prior context clarified; full feasibility ran corpus/feasibility/auditor; followup conversation `batch5-followup-fix-bd07428e` rewrote the prior answer in English without rerunning analysts. |
| Batch5-R2 | Structured markdown final answer with separated citations | done | 2026-06-12 | Final visible text is markdown sections with raw refs stripped; citations are returned separately under the final artifact. Cloud feasibility smoke had required sections, `citations_count=4`, no raw `raw_docs` leak, no JSON shell, and `answer_delta` concatenation exactly matched `final.text`. |
| Batch5-R3 | Azure Redis feasibility cache with data-sensitive key | done | 2026-06-12 | Azure Managed Redis `redis-dataforge-dev` was provisioned and connected via Container App secret `redis-url`. Cache key includes workspace, content fingerprint, prompt version, retrieval mode, and query hash. Local same-query demo-corpus miss/store was 67.799s and hit was 2.484s with same result hash; different workspace `upload-cn-5e0d03538a-20260612065355` missed with a different fingerprint/result; cloud demo-corpus hit was 9.409s with Redis elapsed 20ms. Redis unavailable/unconfigured falls back to direct compute with explicit status. |
| Batch5-R4 | Named/described multi-file upload workspace contract | done | 2026-06-12 | `POST /api/upload` accepts optional `name`, `description`, and multiple `file` parts into one workspace; workspace list/detail include description and document metadata. Cloud upload workspace `upload-cn-5e0d03538a-20260612071456` returned `indexed_count=3`, three documents, and the user-provided name/description from upload, list, and detail APIs. |
| Batch5-Deploy | Backend rollout for Batch5 | done | 2026-06-12 | Deployed `acrdataforgedev.azurecr.io/dataforge-backend:batch5-20260612-json-followup-fix` to revision `ca-dataforge-backend--0000037`; `web/` remained untouched. |
| WP-Onboarding | Upload onboarding, workspace listing, and LLM Chinese guidance | done | 2026-06-11 | Pulled latest `main` and kept `web/` untouched. Added `POST /api/upload` for CSV/JSON/Excel/MD auto-detection, profile-only indexing, and `{workspace_id,name,format,indexed_count,profile_summary}` response; added `GET /api/workspaces` with `doc_count`; replaced hard-coded clarify copy with coordinator LLM Chinese contextual guidance. `python eval/onboarding_upload_eval.py` passed: CSV/JSON/Excel uploaded and searchable, two workspace conclusions differed, three `你好` clarify questions were all `coordinator_llm` and not identical. Deployed backend image `dataforge-backend:onboarding-20260611-172939`; cloud health, upload/search/workspaces smoke, and cloud `你好` LLM guidance passed. |
| WP-R3-hotfix | Evidence ref normalization and nonfatal verification | done | 2026-06-11 | Pulled `main` at `9ed6beb` and kept `web/` untouched. `_verify_evidence` now accepts source-matched reordered refs such as `source#sheet#row#chunk`, prunes invalid evidence instead of raising, and falls back conservatively if model validation still fails. `python eval/ref_verification_regression.py` sent 5 demo-corpus product-feasibility runs with `total_errors=0`; result saved to `docs/ref_verification_regression.json`. Deployed backend image `dataforge-backend:ref-verify-20260611-154007`; cloud health passed and one cloud `/api/chat` smoke had `error_count=0`, `mode=foundry_agent_service`, and 5 dimensions. |
| U3 | Grounded Chinese answer streaming | done | 2026-06-11 | Added model-generated grounded answer writer after audit/revision. It streams `event: answer_delta` and sends `final.text` as the exact concatenation fallback. Verified direct orchestration with 539 deltas and `delta_equals_final=true`; local server long-stream health after first `answer_delta` had max `/api/health` latency 0.02s. |
| U4 | Blob-backed upload workspaces and delete | done | 2026-06-11 | `POST /api/upload` now persists raw file, `profile.json`, `workspace.json`, and Blob registry; `GET /api/workspaces` merges built-ins with Blob registry; `DELETE /api/workspaces/{id}` blocks built-ins and removes Search docs, Blob prefix, registry entry, and local cache. Verified CSV/JSON/Excel uploads searchable, local-cache deletion still listed/searchable from Blob, and delete removed Search/Blob. |
| U6 | Generic cross-signal profiler | done | 2026-06-11 | Added data-driven numeric-by-category differences, category co-occurrence, time trends, and low-discrimination noise detection. Verified two different datasets surfaced different concrete cross-signals, with dates used only for trends. |
| U7 | Foundry-native market web search | done | 2026-06-11 | Market step keeps MCP competitor lookup but separately verifies and runs Foundry-native web search. Bing Grounding is preferred when a connection id is configured; current project verified `web_search_preview` on `gpt-5.1`. External web findings and sources are marked `market_inferred`, and trace emits `foundry_native_web_search` with mode/source metadata. |
| U8 | Azure AI Search hybrid vector retrieval | done | 2026-06-11 | Added 1536-dim `content_vector` field, HNSW vector profile, embedding enrichment via `text-embedding-3-small`, and hybrid query path in `rag.py` with keyword/local fallback. Online schema migration succeeded; demo/excel docs were re-uploaded with vectors; semantic and keyword queries returned `retrieval_mode=hybrid`. |
| U5 | Producer-only endpoint | done | 2026-06-11 | Added optional `POST /api/produce` to run producer artifacts from an existing feasibility/corpus/market payload without rerunning the six-agent chain. |
| Deploy | Backend cloud rollout for U3/U4/U6/U7/U8/U5 | done | 2026-06-11 | Deployed `acrdataforgedev.azurecr.io/dataforge-backend:u3-u8-20260611-2046` to revision `ca-dataforge-backend--0000023`. Cloud health passed; demo-corpus semantic query returned `retrieval_mode=hybrid`; cloud CSV upload was searchable on first retry and delete removed Search and 3 Blob objects; cloud `/api/chat` market trace emitted `foundry_native_web_search` with 4 sources and no errors. |
| Batch4-S1 | Long-stream stability and cloud stream verification | done | 2026-06-12 | Backend revision `ca-dataforge-backend--0000034` streams with 8s heartbeat/progress cadence, coalesced answer deltas, reduced answer length, overlapped/cached market research, parallel producer tools, and narrower audit revision retries. True Chromium/Playwright browser validation ran 5 consecutive cloud `/api/chat` streams with zero network errors, zero `event:error`, `answer_delta` concatenation equal to `final.text`, total durations 36.755s-42.744s, and max chunk gap 8.115s. Current ACA environment has no Premium ingress config, so timeout remains default 240s; the run is now comfortably below that limit. |
| Batch4-S2 | True dependency health probes | done | 2026-06-12 | `/api/health` now actively probes Foundry, MCP, Speech, and Blob with short timeout plus cached results while preserving bool `dependencies`. Cloud revision `0000034` returned all true; cold request was 2.213s and cached requests were 0.717s and 0.453s. Local negative Speech-key check returned `speech=false`, proving failures are not hidden by env-var presence. |
| Batch4-S3 | Blob-persisted conversations and context continuation | done | 2026-06-12 | Added Blob-backed conversation registry/messages plus `GET /api/conversations`, `GET /api/conversations/{conversation_id}`, and real `conversation_id` continuation for `/api/chat`. Cloud smoke `batch4-continuity-1781241492022` produced two successful turns, persisted 4 messages with roles `user,assistant,user,assistant`, listed under `demo-corpus`, and the second assistant response continued from the first turn's sales-operations idea before comparing customer-support automation. |
| WP0 | Repository, `.gitignore`, reuse manifest skeleton | done | 2026-06-10 | Structure check passed: required repository paths exist. |
| WP1 | Terraform application resources | done | 2026-06-10 | `terraform apply` succeeded; output JSON contains required resource endpoints and deployment names. |
| WP2 | Demo corpus and workspace-aware RAG index | done | 2026-06-10 | Uploaded 10 chunks to AI Search; smoke retrieved SaaS signals and medical constraints; health diagnosis verdict is `not_yet_feasible`. |
| WP3 | Backend tool service and schemas | done | 2026-06-10 | Deployed backend image `dataforge-backend:wp3-20260610-1517`; `/api/health` 200 and `/api/search-pack-context` returned 3 demo hits. |
| WP4 | Market MCP server | done | 2026-06-10 | Deployed `dataforge-mcp:wp4-20260610-1520`; health, HTTP `market_lookup`, and MCP `tools/call` returned competitor data. |
| WP5 | Speech narration | done | 2026-06-10 | `/api/narrate-summary` returned `mode=azure-speech`; downloaded WAV artifact had RIFF header and 376044 bytes. |
| WP6 | Six Foundry agents, prompts, tool schemas | done | 2026-06-10 | Created 6 Foundry prompt-agent versions; `foundry_get` verified all agents; `responses_smoke` returned non-empty role outputs for all 6 agents. |
| WP7 | Coordinator-driven orchestrator | done | 2026-06-10 | Deployed `dataforge-backend:wp7-20260610-1658`; cloud `/api/chat` routed product vs corpus questions differently, returned `clarify` for vague input, emitted MCP `market_lookup` count 4, and showed auditor revise/pass for health diagnosis. |
| WP8 | Producer artifacts: PDF, image, audio | done | 2026-06-10 | Deployed `dataforge-backend:wp8-20260610-1715`; cloud `full_package` produced downloadable PDF 3260 bytes, PNG 11676 bytes, and Azure Speech WAV 500444 bytes. |
| WP9 | Three-column trace UI | done | 2026-06-10 | React/Vite UI built successfully; Playwright run against local UI produced 3 cloud artifact links, trace count 20, all 6 agents visible, and desktop/mobile screenshots in `docs/assets`. |
| WP10 | Deterministic router, evals, tracing | done | 2026-06-10 | Routing eval passed 10 cases with accuracy 1.0 and coordinator/router consistency 1.0; deployed `dataforge-backend:wp10-20260610-1728`; App Insights `traces` query returned `dataforge_trace` events. |
| WP11 | End-to-end demo script | done | 2026-06-10 | `python scripts/demo_e2e.py` passed against cloud: clarify, 6 agents, MCP, 3 artifacts, Azure Speech, auditor revise/pass, and honest `not_yet_feasible` all true. |
| WP-A | Real Foundry feasibility and auditor path | done | 2026-06-11 | `python eval/next_stage_wp_a_eval.py` passed: feasibility/auditor emitted real `response_id` and token usage, unsupported non-medical corpus returned weaker verdict, auditor model returned revise for unsupported draft, and orchestrator grep guard passed. |
| WP-B | Local-first Chinese trace UI | done | 2026-06-11 | `npm run build` passed; Browser verified local health probe, no fake starter messages, document click search, tab switching, trace filtering, and model traces with `response_id`/tokens. Screenshots saved in `docs/assets/wp_b_ui_*.png`. |
| WP-C | Excel ingestion and sheet/row grounded search | done | 2026-06-11 | `python eval/next_stage_wp_c_eval.py` passed: Excel rows converted to records, Search schema uses `zh-Hans.microsoft`, cloud search returned concrete `sheet`/`row` hits, excel-corpus produced a distinct real model feasibility report, and cloud backend image `dataforge-backend:next-20260611-2315` passed `/api/chat`. |
| WP-R1/R2 | Post-review RAG graceful degradation and nonblocking SSE | done | 2026-06-11 | Local and cloud `python eval/revise_order_eval.py` passed. Deployed backend image `dataforge-backend:revise-20260611-r1r2b`; no `chinese_hints`, Chinese Excel query returned `sheet`/`row` evidence, zero-hit query returned graceful `not_yet_feasible` without `event:error`, cloud first frame arrived in 0.00005s, and in-stream `/api/health` returned in 0.425s after connection warmup. |
| WP-R3 | Foundry Agent Service self-driven tool loop and user Chinese Excel corpus | done | 2026-06-11 | PR #1 frontend was merged without modifying `web/`. Foundry `df-feasibility-analyst` and `df-auditor` were rebuilt with autonomous `search_pack_context` tool use; `python eval/wp_r3_agent_service_eval.py` passed with both agents in `foundry_agent_service` mode, autonomous SSE tool events, user Excel `sheet`/`row` hits, 189 uploaded `user-excel-corpus` rows, and no duplicate `。。` in empty-evidence final text. Deployed backend image `dataforge-backend:wp-r3-20260611-agent-service`; cloud smoke saved to `docs/wp_r3_cloud_smoke.json`. |
